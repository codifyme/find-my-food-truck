import React from 'react';
import styled from 'style-components';

const MyDesktopNavbar=styled.nav`
    display:flex;
    flex-flow:row nowrap;
    justify-content:space-everyly;
    align-item:center;

    backgroung:red;
    color:white;

    height:15;
    `


const DesktopNavbar=()=>{

        return (
            <div>
                DesktopNavbar
            </div>
    
        );
};

export default DesktopNavbar;